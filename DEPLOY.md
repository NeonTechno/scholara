# SCHOLARA deployment

Target GitHub repository: `NeonTechno/scholara`.

Target Vercel account/team: `neontechno's projects`.

Import the GitHub repository as a **new Vercel project**. Do not reuse or alter DRP projects/domains.

Build command: `npm run build`

Install command: `npm install`

Framework: Next.js
